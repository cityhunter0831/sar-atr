# SAR-ATR 팀 코워크 작업명세서 (v3)
### Claude Cowork · Claude Code · Google Colab · GitHub 활용 가이드라인

**대상**: 권영준, 박윤준 (SAR-ATR 조별과제, Geng et al. 2023 기반)
**기간**: 약 3주
**실험 구성**: ① 클러터 전이(Table 4) ② PH 추출/산란점 ③ 대비 보정 재현(Figure 1, SAMPLE) + Optuna ④ OOD 탐지(holdout class + SAR-ship OE)

**v3 변경점**:
- **Exp C 데이터 전략 전환**: MSTAR El=17°/30° → SAMPLE synthetic/measured (공개 확인, 논문 원본 방법 직접 재현 가능)
- **SAMPLE 데이터셋 추가**: `benjaminlewis-afrl/SAMPLE_dataset_public` GitHub에서 즉시 다운로드 가능
- **miniSAR 대체 확정**: miniSAR(비공개) → SAR-ship OE로 대체, 이론적 근거 정리
- **알려진 코드 버그 목록** 신설: Exp B 도메인 불일치, Exp D 데이터 누수
- **Cowork vs Claude Code 역할 분리** 명시

---

## 0. 전체 그림 — 도구가 서로 어떻게 연결되는가

```
[GitHub]  ──코드의 단일 진실 공급원──
   │  ▲
   │  └── push (코드/문서)
   ▼
[Claude Code (터미널)]               [Google Colab]
 실제 코드 작성·수정·실행 도구          GPU 학습 실행 환경
 - 파일 생성, 함수 구현, 버그 수정       - git clone으로 코드 가져옴
 - pip install, git commit              - Google Drive 마운트로 실데이터 접근
 - mock 데이터로 즉시 파이프라인 검증    - 학습 결과를 Drive에 저장
   │                                      │
   ▼                                      ▼
[Claude Cowork (이 창)]             [Google Drive 공유 폴더]
 전략·해석·리뷰 도구                   - 데이터셋 (MSTAR, SAMPLE, SAR-ship, gengzhe2015)
 - "이 방향이 맞아?" 판단              - 실험 결과 (results/, 체크포인트)
 - 논문 해석, 수식 설명                - 두 사람 모두 접근 권한
 - 코드 리뷰, 버그 원인 분석
 - 보고서/슬라이드 문구 작성
 - 프로젝트 전체 맥락 기억(memory.md)
```

### Cowork vs Claude Code — 역할 구분

| | Claude Cowork | Claude Code |
|---|---|---|
| **역할** | 머리 (전략·판단·해석) | 손 (구현·실행·수정) |
| **사용 시점** | "이게 맞아?", "왜 이걸 써?", 에러 원인 파악 | 코드 작성, 파일 수정, 테스트 실행 |
| **예시** | "Exp B 도메인 불일치 어떻게 고쳐?" | "ph_extraction.py에 함수 추가해줘" |

**워크플로우**: Cowork에서 방향 확인 → Claude Code로 구현 → 에러 나면 에러 전문 Cowork에 붙여넣기 → 원인 분석 → Claude Code로 수정

**핵심 원칙**: 코드는 GitHub에, 데이터/결과는 Drive에, 무거운 연산은 Colab에서. 코드를 "모듈 단위"로 잘라서 각자가 실데이터 없이도 독립 개발·테스트 가능하게 합니다.

---

## 1. Google Colab 공유 — Q&A

| 항목 | 공유 가능? | 설명 |
|---|---|---|
| 노트북 파일(.ipynb) | ✅ | Drive 파일이라 "공유" 버튼으로 공동편집(구글독스처럼) 가능 |
| 데이터 파일 | ✅ | 공유 Drive 폴더에 두면 둘 다 같은 경로로 접근 |
| GPU 런타임(실행 인스턴스) | ❌ | **1인 1인스턴스**. 동시에 같은 GPU 인스턴스를 공유해서 실행할 수 없음 |
| 변수/메모리 상태 | ❌ | 체크포인트(.pth) 파일로 Drive를 통해 주고받기 |

**권장**: 노트북 내용은 GitHub 코드로 관리, 각자 작업 시 Colab에서 `git clone` 후 실행. 공유 Drive는 데이터/결과 전용.

---

## 2. Day 0 체크리스트

- [ ] **MSTAR 데이터 신청** (SDMS) — 두 패키지 함께 신청 (1~5영업일, 최우선)
- [ ] **SAMPLE 데이터 다운로드** ← v3 신규, 승인 불필요, 즉시 가능
  ```bash
  git clone https://github.com/benjaminlewis-afrl/SAMPLE_dataset_public
  # png_images/ 폴더를 Drive data/sample/ 로 복사
  ```
- [ ] **SAR-ship 데이터 다운로드** (NRSA 또는 Kaggle 대체본) — Exp D OE용
- [ ] **gengzhe2015/SAR-target-recognition** 다운로드 → Drive `data/clutter_gengzhe/`
- [ ] **GitHub 팀 레포 생성** + Collaborator 추가
- [ ] **Google Drive 공유 폴더** `SAR_ATR_Project` 생성, 둘 다 편집 권한
- [ ] **디렉토리 구조 + `core/` 모듈 골격** (4·5절) GitHub에 미리 세팅
- [ ] **Colab 템플릿 노트북** 작성 후 둘 다 실행 테스트

> MSTAR 승인까지 1~5일 걸리므로, 그 기간 동안 **SAMPLE(즉시 사용 가능)로 Exp C 파이프라인을 먼저 구축**하면 됩니다.

---

## 3. 데이터셋 다운로드 가이드

### SAMPLE ← v3 신규 추가 (승인 불필요, 즉시 사용)

`benjaminlewis-afrl/SAMPLE_dataset_public` (GitHub 공개, Distribution A — Approved for public release)

- **구조**: `png_images/<class>/<azimuth>/measured/*.png` + `synthetic/*.png` 쌍
- **클래스**: MSTAR와 동일한 10개 차량 (BMP2, BTR70, T72, 2S1, BRDM2 등)
- **자세각**: 방위각 10~80° (공개 버전)
- **정규화**: Decibel(`20*log10(x)`) 또는 QPM(`sqrt(x)`) → 0~255 PNG
- **실험 C 용도**: synthetic으로 훈련 → measured로 테스트 = 논문 Figure 1의 **원본 방법**

```bash
git clone https://github.com/benjaminlewis-afrl/SAMPLE_dataset_public data/sample
```

### MSTAR → 신청해야 할 패키지 (v3 우선순위 재조정)

| 패키지 | 내용 | v3 필요 여부 |
|---|---|---|
| **MSTAR Targets** | T72, BMP2, BTR70 — El 15°/17° | **필수**: 실험 B(PH 추출, raw binary 필요), 실험 D holdout |
| **MSTAR/IU Mixed Targets** | 2S1, BRDM2, BTR60, D7, T62, ZIL131, ZSU23-4 — El 15/17/30/45° | **필수**: 실험 D의 10클래스 구성. 실험 C ablation(El 비교) |
| MSTAR Clutter | 클러터 칩 | 선택: gengzhe2015가 이미 가공 이미지 제공 |
| MSTAR/IU T-72 Variants | T72 변형 시리얼 | 불필요 |

**SDMS 신청 주소**: https://www.sdms.afrl.af.mil
- 계정 생성 → MSTAR collection 검색 → 두 패키지 동시 신청
- 외국 기관도 신청 가능, 승인 후 FTP/HTTP 다운로드 링크 이메일로 전송

**MSTAR raw binary 파일 형식**: Phoenix ASCII 헤더 + 복소수 float32 페이로드
- 확장자: `.015` (El=15°), `.017` (El=17°), `.030` (El=30°) 등
- Exp B에서만 raw binary 필요, 나머지 실험은 PNG 변환본으로 충분

### SAR-ship

**Kaggle 다운로드 (권장)**: https://www.kaggle.com/datasets/search?q=SAR+ship
- "SSDD SAR Ship Detection Dataset" 또는 "SAR-Ship-Dataset" 검색
- Kaggle 계정 로그인 → Download → ZIP 해제

> v2와 달리 Mixed Targets가 Exp C의 **유일한 소스가 아닙니다** — SAMPLE이 Exp C의 주 데이터입니다. Mixed Targets는 Exp D와 Exp C ablation에만 씁니다.

---

## 3-1. 데이터 저장 위치 확정 (v3 신규)

### 로컬 PC — 개발·디버깅 전용 (mock 데이터만)
```
D:\workspace\software\sar-atr\data\   ← .gitignore
```
실데이터를 로컬에 두지 않습니다. Colab GPU 세션에서 매번 업로드해야 하므로 비효율적입니다.

### Google Drive — 실데이터 전용
```
내 드라이브/SAR_ATR_Project/
├── data/
│   ├── clutter_gengzhe/             ← Exp A (gengzhe2015 ZIP 해제)
│   │   ├── Original MSTAR Images/
│   │   ├── Train_OR_Test_CT/
│   │   ├── Train_CT_Test_CT/
│   │   └── Train_CTx2_Test_CT/
│   │
│   ├── sample/                      ← Exp C (git clone 후 복사)
│   │   └── png_images/
│   │       ├── BMP2/
│   │       │   ├── measured/
│   │       │   └── synthetic/
│   │       └── ... (클래스별)
│   │
│   ├── mstar/                       ← Exp B + D (SDMS 승인 후)
│   │   ├── targets/
│   │   │   ├── BMP2/
│   │   │   │   ├── train/           El=15° .015 파일들
│   │   │   │   └── test/            El=17° .017 파일들
│   │   │   ├── BTR70/
│   │   │   └── T72/
│   │   └── mixed_targets/
│   │       ├── 2S1/
│   │       │   ├── train/
│   │       │   └── test/
│   │       ├── BRDM2/
│   │       ├── BTR60/
│   │       ├── D7/
│   │       ├── T62/
│   │       ├── ZIL131/
│   │       └── ZSU23-4/
│   │
│   └── sarship/                     ← Exp D OE (Kaggle 다운로드)
│       └── images/                  PNG 파일들
│
└── results/                         ← 실험 결과 (Colab에서 저장)
    ├── exp_a/
    ├── exp_b/
    ├── exp_c/
    └── exp_d/
```

### SDMS에서 받은 파일 → Drive 정리 방법
1. SDMS에서 받은 ZIP/tar 파일을 로컬에서 해제
2. 클래스별 폴더 확인 후 `train/` (El=15°) / `test/` (El=17°) 로 구분 정리
3. Drive `data/mstar/targets/<클래스>/train|test/` 에 업로드
4. 각 파일명: `HB03344.015`, `HB03345.017` 형태 — 그대로 유지

### SAR-ship → Drive 정리 방법
1. Kaggle ZIP 해제 → 이미지 파일들 확인
2. Drive `data/sarship/images/` 에 통으로 업로드 (클래스 구분 불필요, OE용이므로)

### SAR-ship (miniSAR 대체)

NRSA 또는 Kaggle 대체본 → 실험 D의 Outlier Exposure(OE)용.

**왜 miniSAR 대신 SAR-ship인가**: 논문 원본은 Sandia 비공개 miniSAR를 OE로 사용했습니다. miniSAR의 역할은 "다른 센서 이미지를 훈련 중 노출 → 모델이 미지 도메인에 과신하지 않도록 학습"입니다. SAR-ship은 선박(비차량) + 이기종 센서라는 이중 도메인 차이를 가지므로 miniSAR에 준하는 OE 역할을 합니다. 논문의 선행 연구에서도 SAR-ship OE 선례가 있습니다.

---

## 4. 프로젝트 디렉토리 구조 (업데이트)

```
sar_atr_project/
├── data/                          # Drive에만 (.gitignore)
│   ├── mstar/
│   │   ├── targets/                # MSTAR Targets 패키지
│   │   └── mixed_targets/          # MSTAR/IU Mixed Targets (El17/30 포함, Exp D용)
│   ├── sample/                     # ★v3 신규★ SAMPLE dataset (즉시 사용 가능)
│   │   ├── png_images/             # measured/ + synthetic/ 쌍
│   │   └── mat_files/
│   ├── sarship/
│   └── clutter_gengzhe/
│
├── core/                           # ★v2 신규★ 모든 실험이 공유하는 "계약" 모듈
│   ├── interfaces.py               # SARSample, SARDataset, TrainConfig, EvalResult 등
│   ├── mock_data.py                # 실데이터 없이 테스트할 가짜 데이터셋
│   ├── models.py                   # SMPL, ResNet18, get_model()
│   ├── train.py                    # train_model(model, ds, ds, config) -> EvalResult
│   └── evaluate.py                 # evaluate(model, ds) -> EvalResult, AUROC/TNR 포함
│
├── augmentation/
│   ├── boundary_blend.py           # 권승주 → SSIM/feathering/Poisson blending
│   ├── ph_extraction.py            # 박승준 → PH추출/연대적
│   └── contrast_balance.py         # 박승준 → CLAHE 기반 밝기보정 (Figure1용)
│
├── experiments/
│   ├── exp_a_clutter_transfer.py   # 권승주
│   ├── exp_b_ph_scattering.py      # 박승준
│   ├── exp_c_contrast_optuna.py    # 박승준
│   └── exp_d_ood.py                # 권승주
│
├── notebooks/                       # Colab용 thin wrapper
├── results/
├── docs/
│   └── 이 가이드라인.md
├── requirements.txt
└── .gitignore
```

---

## 5. 모듈 인터페이스 계약 — AI에게 "이 부분만" 맡기는 법

**문제 상황**: 두 사람(또는 각자의 AI 어시스턴트)이 같은 코드베이스를 동시에 건드리면, 서로의 미완성 코드 때문에 막히기 쉽습니다. **해결책: `core/`에 "계약(인터페이스)"을 먼저 정의 → 각 실험 모듈은 이 계약에만 의존 → 다른 모듈의 작성 여부와 무관하게 독립 개발/테스트 가능.**

### 5.1 `core/interfaces.py` (Day 1, 공동 작성 → 가장 먼저)

```python
from dataclasses import dataclass, field
from typing import Protocol, Optional
from torch import Tensor
from torch.utils.data import Dataset

@dataclass
class SARSample:
    image: Tensor          # [1, H, W], float32, [0,1] 정규화
    label: int              # 클래스 인덱스
    meta: dict = field(default_factory=dict)  # azimuth, elevation, class_name, source 등

class SARDataset(Dataset):
    """모든 데이터셋이 이 인터페이스를 따른다."""
    def __getitem__(self, idx) -> SARSample: ...
    def __len__(self) -> int: ...
    @property
    def class_names(self) -> list[str]: ...

class Augmentation(Protocol):
    """클러터전이/밝기보정/경계블렌딩 → 모두 동일 시그니처"""
    def __call__(self, image: Tensor, meta: dict) -> Tensor: ...

@dataclass
class TrainConfig:
    model_name: str            # "smpl" | "resnet18"
    num_classes: int
    epochs: int = 60
    batch_size: int = 128
    lr: float = 1e-3
    lr_decay_epoch: int = 50
    lr_decay_factor: float = 0.1
    loss_type: str = "lsm"     # "lsm" | "at"
    label_smoothing: float = 0.1
    seed: int = 0

@dataclass
class EvalResult:
    accuracy: float
    confusion_matrix: list[list[int]]
    per_class_accuracy: dict[str, float]
    auroc: Optional[float] = None
    tnr_at_95tpr: Optional[float] = None
```

### 5.2 `core/mock_data.py` (Day 1, 공동) → MSTAR 승인 대기 중에도 모든 모듈 테스트 가능

```python
import torch
from core.interfaces import SARSample, SARDataset

class MockSARDataset(SARDataset):
    """64x64 가짜 SAR 데이터 → 파이프라인/모델 검증용"""
    def __init__(self, n=200, num_classes=10, image_size=64):
        self.n, self.num_classes, self.image_size = n, num_classes, image_size

    def __len__(self):
        return self.n

    def __getitem__(self, idx):
        label = idx % self.num_classes
        img = torch.randn(1, self.image_size, self.image_size) * 0.3 + 0.5
        img[:, 28:36, 28:36] += 1.0  # 가짜 "표적" 반사 영역
        return SARSample(image=img.clamp(0, 1), label=label,
                          meta={"class_name": f"class{label}", "source": "mock"})

    @property
    def class_names(self):
        return [f"class{i}" for i in range(self.num_classes)]
```

### 5.3 `core/train.py`, `core/evaluate.py` — 시그니처만 먼저 고정

```python
# core/train.py
def train_model(model, train_ds: SARDataset, val_ds: SARDataset,
                 config: TrainConfig) -> tuple[nn.Module, EvalResult]: ...

# core/evaluate.py
def evaluate(model, test_ds: SARDataset) -> EvalResult: ...
def evaluate_ood(model, id_ds: SARDataset, ood_ds: SARDataset,
                  method: str = "mahalanobis") -> EvalResult: ...
```

### 5.4 AI 작업 단위 표 — "이 모듈만 내줘"라고 요청할 때 쓰는 단위

| 모듈 | 담당 | AI 작업 지시 핵심 | 실데이터 없이 작업? |
|---|---|---|---|
| `core/interfaces.py` | 공동 | ↑ dataclass/Protocol 정의 | ✅ |
| `core/mock_data.py` | 공동 | ↑ MockSARDataset 구현 | ✅ |
| `core/models.py` | 공동 | SMPL + ResNet18(1채널) + `get_model()` (6절 코드) | ✅ |
| `core/train.py` / `evaluate.py` | 공동 | TrainConfig→EvalResult, mock_data로 단위테스트 | ✅ |
| `augmentation/boundary_blend.py` | 권승주 | gengzhe2015 이미지 → feathering/Poisson blending → SSIM | gengzhe2015 zip만 있으면 OK |
| `experiments/exp_a_clutter_transfer.py` | 권승주 | core.train으로 4조건×2모델 학습 → metrics.json | gengzhe2015로 즉시 |
| `experiments/exp_d_ood.py` | 권승주 | pytorch-ood ODIN/Mahalanobis 대입, holdout 구성 | MSTAR 도착 후 (mock으로 골격 선 작성 가능) |
| `augmentation/ph_extraction.py` | 박승준 | MSTAR raw → 2D FFT → 연대적 좌표 추출 | MSTAR raw 필요 |
| `augmentation/contrast_balance.py` | 박승준 | CLAHE 기반 밝기보정 함수 | mock 이미지로 함수 골격 선 작성 가능 |
| `experiments/exp_c_contrast_optuna.py` | 박승준 | Optuna objective = exp_c 정확도 | Mixed Targets 필요 |
| `gradcam/scatter_overlap.py` | 박승준 | Grad-CAM 히트맵 vs 연대적 좌표 비교 | core + ph_extraction 완성 후 |

**사용법**: AI(Antigravity)에게 작업을 줄 때 "`core/interfaces.py`의 `SARDataset`, `TrainConfig`, `EvalResult`를 그대로 따르는 `exp_a_clutter_transfer.py`를 작성해줘. 테스트는 `core/mock_data.py`의 `MockSARDataset`으로"라고 지시하면, 그 AI는 다른 모듈의 실제 구현을 몰라도 됩니다 — 인터페이스만 알면 됩니다.

---

## 6. SMPL + ResNet18 — 왜 둘 다, 어떻게

### 왜
- **SMPL은 논문의 "headline 결과" 모델**입니다 (Table3 56.6%→96.4%, +39.8%p → 여러분 슬라이드 9장 핵심 수치). ResNet18만 구현하면 이 핵심 주장을 직접 검증하지 못합니다.
- **ResNet18은 ImageNet 사전학습이 가능한 대형 모델**이라, "증가 효과"가 "사전학습/대형 구조 덕분"일 수 있습니다. 두 모델을 같은 조건에서 비교해야 "증가 기법 자체의 효과"를 분리할 수 있습니다(ablation).
- 교수님 입장에서는 "논문이 직접 설계한 경량 모델(SMPL)까지 구현하는가"가 **직접 재현 여부를 판단하는 기준**이 됩니다.

### 어떻게 → `core/models.py`

```python
import torch.nn as nn
import torchvision

class SMPL(nn.Module):
    """한 가이드라인 논문 코드 (LeNet 스타일 경량 CNN)"""
    def __init__(self, num_classes=10):
        super().__init__()
        self.features = nn.Sequential(
            nn.Conv2d(1, 16, 5, padding=2), nn.ReLU(), nn.MaxPool2d(2),
            nn.Conv2d(16, 32, 5, padding=2), nn.ReLU(), nn.MaxPool2d(2),
            nn.Conv2d(32, 64, 5, padding=2), nn.ReLU(), nn.MaxPool2d(2),
            nn.Conv2d(64, 128, 5, padding=2), nn.ReLU(),
        )
        self.classifier = nn.Sequential(
            nn.AdaptiveAvgPool2d(1), nn.Flatten(),
            nn.Linear(128, num_classes)
        )
    def forward(self, x):
        return self.classifier(self.features(x))


def get_resnet18(num_classes=10):
    """SAR 1채널 그레이스케일에 맞춰 첫 conv 수정"""
    model = torchvision.models.resnet18(weights=None)
    model.conv1 = nn.Conv2d(1, 64, kernel_size=7, stride=2, padding=3, bias=False)
    model.fc = nn.Linear(model.fc.in_features, num_classes)
    return model


def get_model(name: str, num_classes: int = 10) -> nn.Module:
    if name == "smpl":
        return SMPL(num_classes)
    if name == "resnet18":
        return get_resnet18(num_classes)
    raise ValueError(f"unknown model: {name}")
```

두 모델 모두 `forward(x) -> logits[B, C]`로 동일하므로, `core/train.py`는 `config.model_name`만 바꾸면 그대로 둘 다에 적용됩니다 (코드 분기 불필요).

### 어디에 적용하는가

| 실험 | 모델 | 비고 |
|---|---|---|
| **A (클러터 전이, Table4)** | **SMPL + ResNet18 둘 다 (필수)** | 4조건×2모델 = 8개 결과 → 비교표. 교수님 피드백의 직접 대상 |
| C (Figure1 밝기보정) | ResNet18 (논문 기준) + **SMPL (우리 확장)** | "밝기=학습특징" 문제가 경량모델에서 더/덜 심각한가 → 새로운 분석 포인트 |
| B, D | A에서 학습된 모델 재사용 (둘 중 하나로 충분, 시간 사정에 둘 다) | Grad-CAM/OOD는 백본 선택에 덜 민감 |

---

## 7. Colab 템플릿 코드

```python
# === Cell 1: 환경 설정 (모든 노트북 공통) ===
from google.colab import drive
drive.mount('/content/drive')

DRIVE_ROOT = '/content/drive/MyDrive/SAR_ATR_Project'
REPO_URL = 'https://github.com/<your-org>/sar_atr_project.git'

import os
if not os.path.exists('/content/repo'):
    !git clone {REPO_URL} /content/repo
%cd /content/repo
!git pull
!pip install -r requirements.txt --quiet -q

import sys
sys.path.append('/content/repo')

!ln -sf {DRIVE_ROOT}/data /content/repo/data
!ln -sf {DRIVE_ROOT}/results /content/repo/results
```

```python
# === Cell 2: 실험 실행 (실험 A, SMPL + ResNet18 둘 다) ===
from core.models import get_model
from core.train import train_model
from core.interfaces import TrainConfig
from experiments.exp_a_clutter_transfer import load_condition

for model_name in ["smpl", "resnet18"]:
    for condition in ["MSTAROR", "TrainOR+TestCT", "TrainCT+TestCT", "TrainCTx2+TestCT"]:
        train_ds, test_ds = load_condition(condition)
        model = get_model(model_name, num_classes=5)
        config = TrainConfig(model_name=model_name, num_classes=5, seed=0)
        model, result = train_model(model, train_ds, test_ds, config)
        # metrics.json 저장 (생략)
```

세션 종료 시 마무리:
```python
%cd /content/repo
!git add results/*/metrics.json results/*/*.png
!git commit -m "exp: A 클러터전이 SMPL+ResNet18 seed0"
!git push
```

---

## 8. Git 협업 규칙

### 브랜치 (모듈 단위로 정렬)

| 브랜치 | 담당 | 대상 모듈 |
|---|---|---|
| `feature/core-interfaces` | 공동(Day1) | `core/interfaces.py`, `mock_data.py`, `models.py`, `train.py`, `evaluate.py` |
| `feature/exp-a-clutter` | 권승주 | `experiments/exp_a_clutter_transfer.py` |
| `feature/ssim-boundary` | 권승주 | `augmentation/boundary_blend.py` |
| `feature/exp-d-ood` | 권승주 | `experiments/exp_d_ood.py` |
| `feature/exp-b-ph` | 박승준 | `augmentation/ph_extraction.py`, `experiments/exp_b_ph_scattering.py` |
| `feature/exp-c-contrast-optuna` | 박승준 | `augmentation/contrast_balance.py`, `experiments/exp_c_contrast_optuna.py` |
| `feature/gradcam-validation` | 박승준 | `gradcam/scatter_overlap.py` |

### 규칙
- `core/`는 **Day1에 둘이 함께 만들고 즉시 main에 merge** → 이후 변경 시 상대에게 미리 공지
- 각자 브랜치는 `core/`의 함수를 **호출만** 하고 수정하지 않음 (수정 필요시 PR + 리뷰)
- 커밋 prefix: `feat:`, `fix:`, `exp:`(결과), `docs:`

---

## 9. 확정된 실험 계획 및 완료 기준

| 실험 | 데이터 | 모델 | 완료 기준 |
|---|---|---|---|
| **A. 클러터 전이** (Table4) | gengzhe2015 4종 이미지 | **SMPL + ResNet18** | 4조건×2모델, 3 seed 평균±표준편차, 논문값 비교표 |
| **B. PH/산란점** | MSTAR raw (Targets 패키지) | - | 상위 K개 **공간 도메인** 산란점 에너지 비율 (3개 표적 이상) |
| **C-1. 대비보정+Optuna** (Figure1, 주) | **SAMPLE** synthetic→measured | ResNet18 (+SMPL 확장) | 97.2→65.3→88.5% 트렌드 재현 + Optuna로 88.5% 갱신 시도 |
| **C-2. El 고도각 ablation** (보조) | MSTAR Mixed Targets El17°/30° | ResNet18 | 동일 대비보정이 El 차이에도 유효한지 비교 |
| **D. OOD** | MSTAR 10클래스(holdout J=1~3) + SAR-ship(OE) | A에서 학습된 모델 재사용 (train/test 80/20 분리 필수) | ODIN vs Mahalanobis, AUROC/TNR@95TPR 1조합 이상 |
| **+SSIM** | 실험 A 이미지 | - | 경계 SSIM: 원본교체 vs feathering vs Poisson blending |
| **+Grad-CAM** | A·B·C 결과 | - | 히트맵 vs **공간 도메인** 산란점 위치 일치도 (IoU/상관계수), 샘플 2개+ |

---

## 10. 3주 일정 및 역할

| 주차 | 권영준 | 박윤준 | 공동 |
|---|---|---|---|
| **Day0~1** | — | — | `core/` 5종 모듈 + mock_data 테스트 통과. **SAMPLE 즉시 다운로드** (MSTAR 대기 불필요) |
| **1주차** | 실험 A: gengzhe2015로 SMPL+ResNet18 4조건 학습 | 실험 B: PH추출/산란점 시각화 (공간 도메인 주의). Exp C SAMPLE 로더 구축 | Day0 체크리스트, 코드 버그 목록 수정 |
| **2주차** | 실험 A 비교표 완성 → SSIM 시작 | 실험 C-1: SAMPLE synthetic→measured 재현 + Optuna | 실험 D용 MSTAR 데이터 준비 |
| **3주차** | SSIM 완성, 실험 D 실행 | C-2 El ablation, Grad-CAM 검증 | 전체 비교표 통합, 보고서/발표 준비 |

---

## 11. Claude 활용 가이드

**Cowork (이 창) — 판단·해석·리뷰**
- 오류 원인 분석: "실험 X에서 [파일명] 실행 중 [오류 전문]" 형태로 붙여넣기
- 논문 해석: 슬라이드/Figure/Table 번호 명시
- 전략 질문: "이 방향이 맞아?", "왜 이걸 써?"
- 프로젝트 맥락 유지 (memory.md 자동 참조)

**Claude Code (터미널) — 구현·수정·실행**
- 모듈 작성: "5.4절의 [모듈명]을 `core/interfaces.py` 기준으로 작성해줘"
- 버그 수정: "13절 버그 목록 1번 고쳐줘"
- 테스트 실행: "mock 데이터로 exp_b 파이프라인 실행해줘"

---

## 12. 참고 레포

| 레포 | 용도 |
|---|---|
| [gengzhe2015/SAR-target-recognition](https://github.com/gengzhe2015/SAR-target-recognition) | 실험 A 필수 — Table4 이미지 4종 |
| [benjaminlewis-afrl/SAMPLE_dataset_public](https://github.com/benjaminlewis-afrl/SAMPLE_dataset_public) | **실험 C 필수 (v3 신규)** — synthetic/measured 쌍 PNG |
| [jangsoopark/AConvNet-pytorch](https://github.com/jangsoopark/AConvNet-pytorch) | MSTAR 파이프라인 참고 |
| [SenSE-Lab-OSU/mstar_data_aug](https://github.com/SENSE-Lab-OSU/mstar_data_aug) | 실험 B — PH추출 참고 (MATLAB) |
| [kkirchheim/pytorch-ood](https://github.com/kkirchheim/pytorch-ood) | 실험 D 필수 — ODIN/Mahalanobis |
| [jacobgil/pytorch-grad-cam](https://github.com/jacobgil/pytorch-grad-cam) | Grad-CAM |

---

## 13. 알려진 코드 버그 목록 (v3 신규) — Claude Code로 수정 필요

| 파일 | 버그 | 심각도 | 수정 방향 |
|---|---|---|---|
| `augmentation/ph_extraction.py` + `experiments/exp_b_ph_scattering.py` | **산란점 좌표가 FFT 주파수 도메인** — Grad-CAM은 공간 도메인이라 IoU 비교 물리적으로 의미 없음 | 🔴 치명 | `extract_spatial_scattering_centers()` 함수 신설: amplitude 이미지에서 직접 local maxima 추출 |
| `experiments/exp_d_ood.py` | **train_ds와 test_id_ds가 동일 폴더** — 80/20 split 없이 같은 데이터를 훈련/평가에 사용, 데이터 누수 | 🔴 치명 | `exp_a`의 `_split_dataset` 재사용해서 real data도 80/20 분리 |
| `experiments/exp_a_clutter_transfer.py` | gengzhe2015 폴더가 이미 train/test 분리된 구조일 수 있음 — 현재 코드는 전체를 랜덤 80/20 분할 | 🟠 중간 | gengzhe2015 레포 폴더 구조 확인 후 분기 처리 |
| `augmentation/contrast_balance.py` | `make_optuna_objective` 내 `_AugmentedDataset`이 `SARDataset` 미상속 | 🟡 경미 | `class _AugmentedDataset(SARDataset):`으로 수정 |
| `core/models.py` | `get_resnet18`의 `get_features`를 `types.MethodType`으로 인스턴스에 직접 바인딩 — `torch.save` 시 pickle 문제 가능 | 🟡 경미 | ResNet18 wrapper 클래스로 대체 |
| `experiments/exp_c_contrast_optuna.py` | 데이터 경로가 MSTAR Mixed Targets (El=17°/30°) 전용 — SAMPLE 지원 없음 | 🟠 중간 | **v3 전략 변경**: SAMPLE synthetic/measured 로더 추가 |

> **Claude Code 지시 예시**: "알려진 버그 목록 1번(ph_extraction.py)을 수정해줘. `extract_spatial_scattering_centers(amplitude, k=5)` 함수를 추가하고, exp_b_ph_scattering.py의 `analyse_sample`에서 이 함수를 쓰도록 교체해줘."

---

## 14. 주간 체크인
매주 1회: (1) 완료기준 충족 여부 (2) metrics.json 1줄 요약 (3) 다음 주 막힐 것 → Cowork에서 함께 풀기

---

## 15. 트러블슈팅

| 문제 | 해결 |
|---|---|
| Colab 세션 끊김 | 10 epoch마다 체크포인트 Drive 저장, 재시작 시 이어서 |
| pip 충돌 | `requirements.txt` 버전 고정 |
| Drive 마운트 안 됨 | `drive.mount('/content/drive', force_remount=True)` |
| 두 사람 파일 충돌 | 8절 모듈별 브랜치 규칙 준수, `core/` 수정 시 공지 |
| Optuna 너무 느림 | trial 수 축소(~20), trial별 epoch 축소 후 최종 1회만 풀학습 |
| MSTAR 승인 안 와서 막힘 | **SAMPLE은 즉시 사용 가능** — Exp C 먼저 진행. 5.4절 "실데이터 없이 작업" 모듈도 병행 |
| SAMPLE 클래스/폴더 구조 불확실 | `png_images/` 하위 폴더명 확인 후 `ElevationFilteredDataset` 경로 파라미터 조정 |

---

*이 문서는 `docs/이 가이드라인.md`로 GitHub 레포에 포함시켜 항상 참조합니다.*
